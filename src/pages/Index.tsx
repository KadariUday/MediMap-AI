import IcdCodePredictor from "@/components/IcdCodePredictor";

const Index = () => {
  return (
    <div className="min-h-screen bg-gradient-subtle p-6">
      <IcdCodePredictor />
    </div>
  );
};

export default Index;
